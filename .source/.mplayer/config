# Write your default config options here!

# This fixes problems with mplayer stopping at the end of mp3 files
# and falls back to something else if it doesn't exist
ao=alsa,

vfm=ffmpeg
lavdopts=lowres=1:fast:skiploopfilter=all
