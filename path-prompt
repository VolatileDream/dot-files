#!/bin/bash

path-prompt(){

	radioactive="\u2622"
	[ "$PATH" != "$BASE_PATH" ] && echo -e "\e[0;31m${radioactive}\e[m" && exit

	[ -n "$VIM" ] && echo -n "[vim]"

	echo ">"
}

# source trickery!
if [ "$_" = "$0" ]; then
    path-prompt "$@"
    exit $?
fi
